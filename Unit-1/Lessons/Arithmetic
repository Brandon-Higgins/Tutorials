public class Arithmetic {
    public static void main(String [] args) {

        /* In this lesson we will look at how to work with number in Java
         * The commands we will look at will be addition, subtraction, multiplication, division and mod
         */

        int a = 5;
        int b = 6;

        int c = a+b;    // Int c is equal to whatever a + b is... in this case 11
        System.out.println(c);

        int d = a-b;    // Int d is equal to whatever a - b is... in this case -1
        System.out.println(d);

        int e = a*b;    // int e is equal to whatever a * b is... in this case 30
        System.out.println(e);

        int a1= 10;
        int b1= 2;

        int c1 = a1/b1;  // int c1 is equal to a1 DIVIDED by b1... in this case 5
        System.out.println(c1);

        int a2 = 11;
        int b2 = 2;

        int c2 = a2%b2;     // int c2 is equal to a2 MOD b2
                            // The mod command is the remainder of 11 divided by 2
                            // Since 2 goes into 11 5 times with a remainder 1, 1 will be outputted
        System.out.println(c2);
    }
}
